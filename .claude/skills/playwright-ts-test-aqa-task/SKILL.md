---
name: playwright-ts-test-aqa-task
description: Use when the user wants to prepare, describe or plan a test automation task for this Conduit Playwright + TypeScript project before code is written — gathering test cases, an OpenAPI/Swagger contract for API tests, pages saved from the browser (Ctrl/Cmd+S) for UI tests, and requirements, or producing a Jira-style task, ticket or technical assignment for new UI or API tests — including requests like "new AQA task", "let's plan tests for the settings page", "create a task for automating these cases" or "collect artifacts for automation".
---

# AQA task intake (Conduit)

## Overview

Turns scattered inputs — test cases, an API contract or saved pages, requirements — into one self-contained Jira-style markdown task that a person or the [playwright-ts-conduit-realworld](../playwright-ts-conduit-realworld/SKILL.md) skill can implement without asking again.

The skill interviews; it never invents cases, expected results, messages or locators. Anything the user did not provide is written as a draft to confirm or as an open question.

## Output

```
tasks/<KEY>/
  <KEY>.md                          the task, filled from assets/task-template.md
  api/<name>.json|.yaml             OpenAPI/Swagger spec, HAR or request examples — API and hybrid tasks
  pages/<name>.html + <name>_files/ pages saved in full format, one file per state — UI and hybrid tasks
  pages/<name>.md                   page descriptions generated by `npm run page:md`
  cases/, requirements/             files the user provided (copied, never moved)
```

`api/` and `pages/` are filled according to the layer chosen in block 1 — an API-only task has no `pages/`, a UI-only task has no `api/`.

`<KEY>` is the Jira key when the user has one, otherwise `AQA-<yyyyMMdd>-<short-slug>`. Use the user's own location if they name one.

## Interview

Ask **one block at a time** and wait for the answer. Use the AskUserQuestion tool for choices (type, priority, layer) when it is available; otherwise ask in chat with short numbered questions. "Skip" is a valid answer — record it as an open question. After each block, restate what you understood in 2–4 lines before moving on.

### Block 1 — Task frame

1. Jira key (or none), one-line summary, feature/area.
2. Type: new coverage, extend existing tests, fix flaky tests, refactor.
3. Priority, deadline, links (epic, story, bug, designs).
4. **Test layer — ask this explicitly, it decides which artifacts block 3 asks for:** API, UI, hybrid (API arrange → UI steps), mocked UI — or "decide by the test pyramid".
   - "Decide by the test pyramid" → propose the split yourself, name the reason per case, and confirm it before block 3.
   - Mixed answers are normal: a task can be API for the rules and UI for two flows. Record which cases belong to which layer — block 3 then runs both branches.

### Block 2 — Test cases

No cases yet, only requirements? Review the requirements and write the cases first with the [playwright-ts-test-requirements](../playwright-ts-test-requirements/SKILL.md) skill, then continue here with its `test-cases.md`.

Ask for the cases to implement in any form: Qase suite/IDs (read them with the `qase-api` skill when available), a file (csv, xlsx, md), pasted text or a list of titles. Capture per case: ID, title, preconditions, steps, expected result, priority, layer, test data, tags.

- Only titles given → draft steps and expected results from the requirements and saved pages, mark each **Draft — confirm**, and get confirmation before writing the task.
- API cases: walk the [playwright-ts-api-checklist](../playwright-ts-api-checklist/SKILL.md) skill against each operation in scope, so the case list comes from a checklist rather than from what came to mind. Its coverage table drops straight into the task.
- Cases that register or log in a user spend the auth quota (~5 auth requests/hour) — keep them few.
- Point out cases better covered by API than UI, and cases that cannot be automated as stated (and why).

### Block 3 — Artifacts of the chosen layer

Run only the branch the layer from block 1 needs: **API → 3A**, **UI or mocked UI → 3B**, **hybrid → both**. Do not ask for saved pages on an API-only task, and do not ask for an OpenAPI spec on a UI-only task.

#### Block 3A — API contract (API and hybrid)

Ask, in this order:

1. **OpenAPI / Swagger** — a file (`openapi.json`, `swagger.yaml`) or a URL to one. Copy files into `tasks/<KEY>/api/`; for a URL, ask for an export so the task stays self-contained.
2. **No spec?** Then a HAR export, `curl` examples, or real request/response pairs for the endpoints in scope — whatever the user actually has.
3. **Which operations are in scope** — path + method (or `operationId`) per case, so the task does not describe the whole API.
4. Per operation, confirm what the spec does not state clearly: auth (token or guest), request body, success status, error statuses **and the shape of the error payload**.

Warn that HAR files and pasted examples carry tokens, cookies and real user data — ask for a test account and for secrets to be stripped before the file is copied in.

Read the copied spec with the [playwright-ts-test-requirements](../playwright-ts-test-requirements/SKILL.md) skill's input handling (`.json` / `.yaml` contracts are supported there), then map each operation onto the project structure of the `playwright-ts-conduit-realworld` skill and record it in the task: `BasePath` entry, request and response models by domain, endpoint helper `<Domain><Verb>API`, API flow when the case needs arrange or cleanup, and a JSON schema for `expect(model).toMatchSchema(Schema.X)`.

Without a contract **do not invent statuses, field names or error texts** — each unknown becomes an open question of the task.

#### Block 3B — Pages saved in full format (UI, mocked UI and hybrid)

For every page the UI cases touch, ask the user to save it and give these instructions:

1. Open the page in Chrome in the exact state the test needs: guest or logged in, empty or filled form, validation error shown, dialog open. **One saved file per state.**
2. Wait until the content has rendered (the app is a SPA).
3. Press **Ctrl+S** (**Cmd+S** on macOS) and choose **Webpage, Complete** — this saves `<name>.html` plus the `<name>_files/` folder.
4. Put both into `tasks/<KEY>/pages/` (or give the path) and say the route (`#/editor`) and the state.

Warn that saved pages can contain personal data: use test accounts and remove anything sensitive.

Then parse the saved pages into page descriptions with the shared script of the `playwright-ts-conduit-realworld` skill:

```bash
npm run page:md -- "tasks/<KEY>/pages"
```

It needs no server and spends no rate-limit budget, and writes `<name>.md` next to each HTML file: source URL and route, root candidates, elements grouped like page-object maps with suggested names, locators and match counts, forms, repeated items, the existing page object, open decisions and a page-object draft.

Link every `pages/<name>.md` from the task's **Pages and artifacts** section and copy its element tables there. Each open decision of a page description becomes an open question of the task. If a description lists almost no elements, the page was saved before it rendered or as "HTML only": ask for a re-save.

### Block 4 — Requirements

Ask for:

- **What the feature must do:** user story or acceptance criteria, business rules, validation and error messages (**exact texts**).
- **API contract:** only what block 3A did not already cover — rules the spec does not express (ordering of validation errors, idempotency, rate limits).
- **Data and environment:** users and roles, test data, environments, feature flags, browsers or viewport.
- **Boundaries:** out of scope, links to docs.

Copy provided files into `tasks/<KEY>/requirements/`.

### Block 5 — Review before writing

Show a compact summary: cases (ID, title, layer, priority, confirmed/draft), the API operations in scope and/or the pages with their states, key requirements, open questions. Ask whether to write the task. Write only after a yes.

## Writing the task

- Fill [assets/task-template.md](assets/task-template.md) section by section. Remove sections that do not apply; never leave `<placeholders>`.
- Every expected result, message and rule names its source (case ID, file, or "user, block 4"). Drafts stay marked **Draft — confirm**.
- The implementation plan follows the `playwright-ts-conduit-realworld` skill and lists only what the chosen layer needs:
  - spec files `tests/api/*.api.spec.ts` / `tests/ui/*.ui.spec.ts`;
  - API tasks: `BasePath` entries, request/response models, endpoint helpers, flows and response schemas, each naming its operation in the contract;
  - UI tasks: page objects to add or extend, with element names from the page descriptions, and routes;
  - generators and test data;
  - reuse of existing classes, called out explicitly.
- Keep the Definition of done from the template.
- Finish by giving the task path and offering to implement it with the `playwright-ts-conduit-realworld` skill.

## Common mistakes

| Mistake                                                                      | Instead                                                                           |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Writing the task after block 1 with guessed cases                            | Every block answered or skipped; drafts confirmed in block 5                      |
| All questions at once in one long message                                    | One block at a time, short numbered questions                                     |
| Page saved as "HTML only" or before the SPA rendered                         | Re-save as **Webpage, Complete** after the content is visible                     |
| One saved page for every state                                               | One file per state the tests act on or assert                                     |
| Locators or messages taken from memory of the live site                      | Page descriptions generated from the saved pages and texts from the requirements  |
| Inventing expected results or error texts                                    | Mark as open question or **Draft — confirm**                                      |
| Asking for saved pages on an API-only task (or for a spec on a UI-only task) | Block 1 fixes the layer; block 3 runs only the branch that layer needs            |
| Statuses, field names or error payloads written from memory of the API       | Taken from the OpenAPI spec or the examples in `api/`; otherwise an open question |
| HAR or examples copied in with real tokens and cookies                       | Test account, secrets stripped before the file is copied                          |
